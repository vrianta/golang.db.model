DOCS.intro = {
  id: "intro",
  title: "Introduction",
  render(section) {
    section.append(
      UI.paragraph(
        "ModelsHandler is a human-friendly ORM-like query builder for Go focused on clarity, safety, and schema synchronization."
      )
    )
  }
}
